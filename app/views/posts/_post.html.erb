<li class="post">
  <h2><%= link_to post.title, user_post_path(post.user_id, post.id) %></h2>
  <p>Publicado por <%= post.user.name %> <%= time_ago_in_words(post.created_at) %></p>
  <p><%= post.body %></p>
</li>
