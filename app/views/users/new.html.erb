<% provide(:title, I18n.t(:signup)) %>

<%= render 'shared/admin_menu' %>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <h1><%= t :signup %></h1>

        <%= render 'form' %>
        
    </div>
</div>
