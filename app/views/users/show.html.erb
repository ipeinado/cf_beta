<% provide(:title, "#{@user.name}") %>

<div class="row">
    <div class="col-md-4">
        <div class="user_info">
            <%= image_tag @user.avatar_url.to_s %>
            <h1><%= @user.name %></h1>
        </div>
    </div>
</div>
