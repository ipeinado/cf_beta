<% provide(:title, t(:edit_event)) %>

<h1><%= t(:edit_event) %></h1>

<%= render 'form' %>

<%= link_to t(:delete_event), event_path(@event), method: :delete, data: { confirm: t(:confirm_delete_event)}, class: "btn btn-danger" %>
