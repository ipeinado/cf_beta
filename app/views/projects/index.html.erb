<% provide(:title, "Proyectos") %>

<h1>
  Proyectos
  <% if current_user && admin_user %>
    <%= link_to I18n.t(:new_project_title), new_project_path, class: 'btn btn-default' %>
  <% end %>
</h1>

<% @projects.each do |project| %>
  <%= render project %>
<% end %>
